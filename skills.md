# Skills — Empowr Landing Page

| Skill | When to use |
|---|---|
| `/netlify-deploy` | Deploy to Netlify and configure `start.empowrcic.org` |
| `/pre-build-check` | Validate build structure and frontend quality before deploy |
| `/pre-deploy-security` | Run before any deploy — FAILs block the deploy |
| `/webapp-testing` | Test UI at 480px with Playwright after any change |
| `/simplify` | Review changed code for reuse, quality, and efficiency |
